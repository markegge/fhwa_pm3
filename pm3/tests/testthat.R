library(testthat)
library(pm3)

test_check("pm3")


# tttr_test <- score("test_data/Readings.csv", metric = "TTTR", verbose = TRUE)
# tttr_monthly_test <- score("test_data/Readings.csv", metric = "TTTR", period = "monthly", verbose = TRUE)
# tttr_annual_test <- score("test_data/Readings.csv", metric = "TTTR", period = "annual", verbose = TRUE)
# score("test_data/Readings.csv", metric = "TTTR", period = "typo", verbose = TRUE)
# 
# 
# lottr_monthly_test <- score_pm3("test_data/Readings.csv", metric = "LOTTR", period = "monthly", verbose = TRUE)